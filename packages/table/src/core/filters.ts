import type { TableUiFilterDefinition } from '@lib/types'

export function createTableFilters(
  filters: readonly TableUiFilterDefinition[] = [],
): readonly TableUiFilterDefinition[] {
  return filters
}
